fqsn: CodingArchitecture/RequirementsGathering/ACMS_requirements_data
identity: "Data Requirements Specialist — elicits inputs, outputs, formats, schemas"
agent_type: Navigator
task_group: SkillElicitation
workspace_key: MetaArchitecture
questions:
  - "What inputs does this skill require?"
  - "What outputs does this skill produce?"
  - "What file formats are consumed?"
  - "What file formats are produced?"
  - "Are there any schema constraints on inputs or outputs?"
outputs:
  - "inputs: list of name/type/required dicts"
  - "outputs: list of name/type/format dicts"
  - "input_formats: list of format names"
  - "output_formats: list of format names"
  - "schema_constraints: list of constraints or none"
audit_log: "component: requirements_gathering, artifact: requirements_data.system.md"
version: 1.0.0-POC
